# Transformer/Attention プロジェクト

## 翻訳方向(最重要・source of truth / 2026-06-04 確定)

- **本来の目標**: **手話 → 日本語(認識)** が第一。手話動画 → 姿勢/手ポーズ抽出 → タグ列 → (LLM整形) → 日本語、という認識パイプライン。最終ゴールは双方向だが、**先に作るのは認識方向**。
- **日本語 → 手話(生成)は後段**(ロードマップ上 v1.5 以降)。
- **注意(再発防止)**: 2026-06-04 に一時「生成補強(日本語→タグ + ポーズ辞書引き)」へ舵を切った経緯があるが、これは**認識がランドマーク破綻(下記 S6)で詰まったための暫定回避策**であり、**本来の目標ではない**。同日中に認識方向へ戻すと決定(reverted)。コードが「日本語→タグ」中心に見えても、それは Phase 15b の足場であって、目標方向ではない。
- **`transformer_burn` の現状の `日本語→手話タグ` モデルは"足場"**。認識では入力を「日本語トークン列」から「手ポーズ列(ランドマーク列)」に差し替えて `ポーズ列→タグ` モデルにする(Phase 0a 本来の計画)。
- **記録ルール**: 技術的ブロックによる迂回は必ず「暫定回避策(本来の目標は X、ブロック Y のため一時 Z)」と明示し、裸の「方向性決定」として書かない。目標(安定)と戦術(可変)を混同しない。

## 開発計画

- バージョン(v0.1〜v2.0)・フェーズ(Phase 0a〜1+)別の計画表は **`docs/development-plan.md`**(2026-07-02 作成)。短期アクション・判断ポイント・未確定論点もそちらに集約。大きな判断(基準クリア、移行、方針変更)があったら本ファイルと同期する

## データ提供同意書(別リポジトリ)

- 学習データを提供してくれる協力者(ろう者)と結ぶ**同意書・契約管理は別リポジトリ** `github.com/nyk9/attention` ではなく **`github.com/nyk9/nft`**(ローカル `~/ghq/github.com/nyk9/nft`)。2026-08-04 に本リポジトリの `data_consent/` から移設した
- リポジトリ名は `nft` だが、**NFT/ブロックチェーンを使うことは未確定**(調査の結果、優先度は最も低い)。中心は同意書の中身と、チェーン不使用の署名・記録の仕組み
- **本リポジトリの方針に影響する決定(2026-08-04、ユーザーの意向・確定ではない)**: 学習済みモデルは**オープンウェイトとして公開する予定**、ランドマークデータセットも**できれば公開したい**、撤回されてもモデルの作り直しはしない(次のモデル以降で不使用は可能)
- **協力者の募集は未着手で、当初想定より遅れている。最悪の場合ゼロのままリリースに至る可能性もある**。現在の stage 1 / stage 2 のデータはすべて開発者本人によるもの
- 詳細・調査結果・同意書ドラフトは `nft` リポジトリの `README.md` / `CLAUDE.md` を参照

## プロジェクト構成

### 1. transformer_burn - 手話翻訳AI
- 現状: 日本語→手話タグの可変長翻訳(Seq2Seq)= **認識モデルの足場**(本来の目標は手話→日本語認識。上記「翻訳方向」参照)
- 認識化の計画: 入力を「日本語トークン列」→「手ポーズ列」に差し替え、`ポーズ列→タグ` モデルにする
- **ステータス**: Phase 15b完了、Phase 16(モデル保存・推論基盤)90%完了
- **語彙**: 168(日本語ひらがな86 + 手話タグ79 + SOS/EOS/PAD 3)。出典: `transformer_burn/src/config.rs:8`。2026-08-22 訂正: 従来「手話タグ80」と書かれていたが 86+80+3=169 で合計と合わなかった
- **データ**: 47サンプル

### 2. translator_ja_en - 日英翻訳AI
- 日本語→英語の機械翻訳(手話データ準備期間中の学習継続)
- **ステータス**: Phase 1〜3完了(データセット・語彙・モデルスケールアップ)。Phase 4(実用化)はビームサーチ推論まで実装済み、BLEU評価は未着手。**2026-07-02 訂正**: 本節は 2025-11-01 の学習後も約8ヶ月間「初期セットアップ完了」のまま未更新だった(pose_extractor 側の作業に集中していたため)。以下は実態に合わせた最新値
- **語彙**: 日本語168 + 英語798(特殊トークン込み)
- **データ**: **実際に学習に使われているのは `data/translation_data_ja_en.txt` の1ファイルのみ(有効998行)**。`TranslationData::load` が単一ファイルパスを取る構造で、`main.rs:62,101` がこのパスを直接読む。`data/` 配下には他に6ファイル(合計 有効2977行)が置かれているが**読み込まれていない**。2026-08-22 訂正: 従来「2879サンプルを統合」と書かれていたが、統合は実装されておらず件数も一致しない。**未確定**: 「統合する予定だったが未実装」なのか「統合済みのつもりだった」のかで、記述を直すのかコードを直すのかが変わる(ユーザー判断待ち)。なお手話タグ用の `training_data_jsl.txt`(47行)が本ディレクトリに混在している(内容は transformer_burn 側のもの)
- **モデル**: d_model=128、4ヘッド、Encoder/Decoder各4層、d_ff=512、seq_len=30。`models/test/` に150エポック学習済みモデルあり(2025-11-01訓練、final loss 0.162)

### 3. elan_parser - ELAN .eaf アノテーション抽出CLI
- ELAN(手話研究のデファクトアノテーションツール)出力の `.eaf` XML から、gloss 列を TSV/JSON 形式で抽出
- **ステータス**: v1完了(`ALIGNABLE_ANNOTATION` 対応、`REF_ANNOTATION` は v2)
- **使用クレート**: quick-xml 0.36, clap 4, serde, anyhow
- **バイナリ**: `elan-eaf-parse`
- **目的**: ろう者協力者から受け取るELANアノテーションを将来のMLパイプラインに繋ぐ前処理
- **Skill連携**: `~/.claude/skills/elan-eaf-parse/SKILL.md` から自動発見

### 4. pose_extractor - 動画→姿勢ランドマーク抽出CLI
- BlazePose ONNX(MediaPipe Pose 変換版)を `ort` クレートで呼び、動画フレームから39 keypoints(33+6補助)を抽出
- **ステータス**: Phase 0a-pipe-v1 の機能側は **S1〜S8 + 動画→タグ直結パスまで実装完了**(PR #1 で main にマージ、2026-06-14)。対話ウィザード CLI・バッチ処理・撮影進捗ツール対応済み。**先頭10語の実撮影データで Phase 0a 終了基準を判定済み(2026-07-02、実質クリア。下記参照)**。次は stage 2(11-20語目)の撮影へ。**精度上の課題が S6 検証で発見済み(下記参照)**のため、認識は手ポーズ(C 系)中心で進行中
- **使用クレート**: ort 2.0.0-rc.12, ndarray 0.16, image 0.25, dialoguer 0.11, chrono 0.4, serde, anyhow, `transformer_burn`(path 依存、default-features = false)。2026-08-22 訂正: 従来 clap 4 と書かれていたが `pose_extractor/Cargo.toml` に存在しない(2026-06-13 のフラグ廃止で除去されたとみられる)。chrono と transformer_burn は未記載だった
- **外部依存**: ffmpeg 8.1.1(`brew install ffmpeg`、CLI subprocess 経由で動画読み込み)
- **モデル**: `models/blazepose_full.onnx`(5.3MB、HF `opencv/pose_estimation_mediapipe`)。git管理外
- **バイナリ**: `pose-extract`。**CLI はフラグを使わず、引数なし起動のトップレベルメニュー(質問形式)で全機能に入る**(2026-06-13 にフラグ廃止。`--flag` 暗記不要の UI/UX 方針 [[cli-interactive-wizard-preference]])。メニュー項目: 動画抽出 / 撮影セッション / 撮影進捗 / build-dict 用エクスポート / 辞書構築(build-dict)/ 動画→タグ認識(推論)/ dev:inspect / dev:test-infer。設定は dialoguer の Select/Input で対話的に尋ねる。**TTY 必須**(スクリプトからの非対話呼び出しは不可。将来フラグ併設の可能性はあるが判断はユーザーが行う)
- **撮影進捗ツール(2026-06-12 追加、2026-06-13 ウィザード化)**: メニュー「撮影進捗を確認」= `data/raw_jsl` をスキャンして `index.tsv` を自動生成・更新し進捗表示(words.tsv 照合・typo/長さ警告)。メニュー「撮影セッション」= OBS/QuickTime の保存先を監視し、新録画を自動で命名(`<word_id>_<romaji>/<rep>.mp4`)・mp4詰め替え・記帳・手検出チェック。撮影は既製ツールのまま横で取り込む方式(ツール内録画は Phase 0b 以降)。実装は `src/progress.rs`、words.tsv は初回に50語テンプレ自動生成(ろう者協力者レビュー前の暫定)
- **手検出チェックの判定方式を変更(2026-07-02)**: 実撮影(先頭10語「こんにちは」)で4テイク連続 `ng_hands` が発生。オーバーレイ用に抜いたフレームを目視した結果、クロップ/照明は問題なく、**一人撮影のため録画の前後に手を下げた時間が長く**(サイン自体は動画の半分弱の区間に収まる)、動画全体平均のカバレッジ(旧: 50%未満で`ng_hands`)が構造的に閾値を割ることが原因と判明。**「動画全体の平均」から「連続して手が検出できた最長区間(1.0秒未満で`ng_hands`)」に変更**(`progress.rs`: `HAND_COVERAGE_THRESHOLD` → `MIN_HAND_RUN_SECONDS`+`MAX_RUN_GAP_SAMPLES`、瞬断1サンプルまでは同一区間として橋渡し)。ユニットテスト5件追加。なお `build-dict` は各テイクから均等10フレームを抜くため、手ぶら時間が長いテイクは学習特徴としても質が落ちる点は変わらず、録画時は手ぶら時間を短くする方が望ましい
- **使い方**: `pose_extractor/CLI.md` 参照(videos/ ディレクトリに動画を置く運用)
- **入力仕様(身体 BlazePose モデル、S6 の破綻により現在は不使用)**: NHWC (1, 256, 256, 3) float32 [0, 1] RGB(中央クロップ + 256×256 リサイズ、S6 検証後に stretch から変更)
- **入力仕様(実際に動いている手のパイプライン)**: Palm 検出 = **192×192**(`main.rs:22 PALM_INPUT_SIZE`)、Hand ランドマーク = **224×224**(`main.rs:27 HAND_INPUT_SIZE`)。いずれも ONNX モデルの入力テンソル形状で固定であり変更できない。2026-08-22 追記: 従来「入力仕様」として身体モデルの 256×256 だけが書かれており、**使われていない経路の仕様だけが載っていて実際に動いている手の仕様が無かった**
- **出力仕様**: landmarks (1, 195) = 39 × [x, y, z, visibility, presence] + conf (1, 1) + その他3個
- **TSV出力**: wide 形式(1 行 = 1 フレーム、197 列: frame_idx, confidence, x0..pres38)

### 5. llm_shaping - タグ列→自然な日本語(部品C)
- 認識パイプライン最終段の LLM 整形。認識モデルの top-k(タグ+確率)を自然な日本語文にする
- **ステータス**: ①プロンプト試作完了(2026-07-02)、実データでの独立再検証も完了(2026-08-14)。①プロンプト→②LoRA→③自作の①段階で、コードは無くプロンプト(`prompt_v1.md`)と検証記録(`eval_v1.md`)のみ
- **検証所見**: 1語素通し・過剰生成抑止・不確実性明示(?)は動く。文脈による top-k 挽回は「top-1 の組が意味的に破綻」時のみ発動し、現語彙51語では機会が少ない → **②への投資判断は文(複数サイン)認識が動いてからで十分**。高確信度の単発誤り(左右手ゆれ由来)は部品Cでは救えない(認識側の課題との責任分界を確認)。**実データ独立再検証(2026-08-14)でも同じ結論が再現**、加えて「拮抗?判定の閾値が不明確」という弱点も実データで再確認(v2候補)
- **注意**: プロンプト内語彙表は words.tsv と要同期(現在51語版のまま。2026-08-16 以降 words.tsv が74語に拡張されたため未同期。次に部品Cを触るときに要再同期。word_id 022 は 2026-07-02 に tatoeba/例えば に確定)

#### S6 検証で発見した問題

1920×1080 の手話動画 6 本(各 4-9 秒)をバッチ実行した結果(**2026-08-22 注記**: この 1920×1080 は S6 検証当時[2026-05]に使った動画の解像度であり、**現在の撮影データ `raw_jsl` は全て 1620×1080**。当時の記述としては正しいが、現在の撮影データの説明として読むと誤りになる):

- **stats 上は良好**: 全 6 本で confidence mean = 0.82-0.90
- **しかしオーバーレイで確認すると、39 ランドマークが顔と襟元の極狭い領域に集中** していて、手・肩・腰・膝・足の本来のランドマークが全く見えない(手のジェスチャーをしているフレームでも手の位置にドットが一切ない)

原因(推定): BlazePose の **Pose Landmark モデルは「人物がクロップされた正方形画像」を前提**。MediaPipe 本来のパイプラインは Pose Detection で bbox を取って人物クロップしてから Landmark に渡す 2 段構え。現状は後半のみを使い、1920×1080 をいきなり 256×256 に stretch しているので、モデルから見ると「全身が崩れた変な人物」になる。顔だけは確実に検出できるので confidence 数値だけ高く出る。

#### 改善案と採用方針(2026-05-23 決定)

| 案 | 工数 | 手話用途への適合 | 採用 |
|---|---|---|---|
| A. 中央クロップ(1080×1080 → 256×256) | 小 | 上半身は改善、脚は不可視のまま | **採用(第1段)** |
| B. Pose Detection モデル追加(2段パイプライン) | 中 | MediaPipe 本来のフロー | 不採用(手指が取れない) |
| C. MediaPipe Hands ONNX 併用(手指 21点×2) | 中-大 | 手話に最重要な手指が取れる | **採用(第2段)** |
| D. MediaPipe Holistic(Pose + Hands + Face) | 大 | 表情・手指・上半身姿勢を統合 | 将来採用予定 |

- **採用方針**: 段階的に A → C → 将来 D
  - 第1段: A の中央クロップで BlazePose の上半身ランドマークを正しく取れるようにする
  - 第2段: C で MediaPipe Hands ONNX を追加し、手指 21点×2手を併用
  - 将来: A+C が安定したら D の Holistic 統合(表情・微妙な手指・上半身姿勢を一体化)へ
- **進捗(2026-06-04 時点)**:
  - A: 実装完了。confidence は向上したが身体ランドマークは破綻したまま(人物が正方形内で小さい)
  - C-1(Palm Detection): 完了。SSD anchors 2016 を埋め込み、両手検出可能
  - C-2(Hand Landmark): 完了。21 keypoints/手を取得。**回転アラインメント実装完了(2026-06-20)**: Palm keypoint[0]=手首・[2]=中指MCP を結ぶ向きが上向き(90°)になる回転角を求め(OpenCV Zoo `mp_handpose` / MediaPipe `DetectionsToRectsCalculator` と同式)、クロップを回転して双線形サンプリング→手ランドマークモデルへ。出力は逆アフィンで元画像へ戻す。回転角0のとき従来の軸並行クロップに一致する一般化(後方互換)。中心シフトも回転後フレームの y 軸方向へ適用(MediaPipe RectTransformation 準拠)
  - S7(動画→手ポーズ列 抽出基盤): `build-dict` サブコマンド実装完了。`<タグ名>.mp4` 群から 1 フレーム=126 次元(左右の手 21点×xyz、手のみ。身体ランドマークは破綻のため不採用)の手ポーズ列を抽出。`--frames` でダウンサンプル(既定 10=SEQ_LEN)、座標は width/height で正規化。既存動画 1 本でスモークテスト済み(coverage 約 62%)
- **S7-8 の方向性(2026-06-04: 生成補強を一度採用 → 同日 認識へ戻す reverted)**:
  - 一時、認識ではなく**生成補強**(日本語→タグ + `build-dict` のポーズ辞書引き)を採用したが、これは認識がランドマーク破綻で詰まったための**暫定回避策**。本来の目標(手話→日本語 認識)に戻すため**取り消し**。
  - **現方針 = 認識**: `build-dict` で得た「動画→手ポーズ列(126次元)」は、辞書ではなく**認識モデルの学習データ・入力特徴**として転用する(`(手ポーズ列, タグ)` ペア)。手のみ特徴なので身体ランドマーク破綻を回避できる。
- **S8(認識モデル: 手ポーズ列→タグ)進捗(2026-06-11)**:
  - **学習ループ実装完了・スモーク通過**。transformer_burn に認識モデルを追加(足場の Seq2Seq とは別モデルとして共存)
  - 新規モジュール: `tag_vocabulary.rs`(動的タグ語彙+SOS/EOS/PAD、モデルと一緒に保存)/ `pose_data.rs`(build-dict JSON 読み込み、stem 末尾の `-NN` テイク番号を剥がしてラベル化)/ `recognition.rs`(PoseEncoder = Linear 126→d_model + 既存 TransformerBlock、Decoder は**因果マスク付き** Self-Attention)/ `recognition_training.rs`(Teacher Forcing 学習+top-k 評価)
  - CLI: `--train-pose <dict.json> [--epochs N] [--save dir]` / `--load dir --predict-pose <dict.json>`
  - スモーク結果(既存6動画=2ラベル×3テイク、300エポック・約47秒・WGPU): Loss 4.50 → 0.00、in-sample top-1 6/6。save→load 往復も一致
  - 注意: 2クラス in-sample の 100% は配線確認であって精度の証拠ではない。本判定は撮影後の未見テイクで行う
- **動画→タグ直結パス 完了(2026-06-14、PR #1 で main にマージ)**:
  - pose-extract メニュー「動画からタグを認識(推論)」。pose_extractor が transformer_burn を lib 依存し、動画→手ポーズ列(126次元)→ 認識モデル(CPU/NdArray)→ top-k タグを **1 プロセスで完結**(S7 抽出 + S8 推論を結線)。学習は引き続き build-dict→`--train-pose`。
  - E2E スモーク通過(既存6動画+rec_smoke、0205-01.mp4 → top-1 "0205" 確率1.0)。回帰用に `#[ignore]` テスト `recognize_smoke_in_sample` 同梱(`cargo test recognize_smoke_in_sample -- --ignored`)。
  - 同 PR で checkpoint クロスバックエンドテストのバグ修正、レビュー指摘3件(overlay範囲 / フレームレート0除算 / TSVサニタイズ)も対応。大きい/private な生成物(`models/rec_*`・`data/pose_dict*.json`・`data/raw_jsl/`)は gitignore で非追跡。
  - 注意: in-sample の一致は配線確認であって精度の証拠ではない。本判定は撮影後の未見テイクで。
- **ビルド軽量化(feature ゲート化)完了(2026-06-19)**:
  - 直結パス導入時の代償だった「pose_extractor の初回ビルドが Burn(wgpu)取り込みで重い」を解消。transformer_burn 側で **wgpu(+autodiff)を `wgpu` feature でゲート**(`default = ["wgpu"]`、`ndarray` は CPU 推論で常用するため常時有効)。wgpu に触れるモジュール(checkpoint / inference / training / recognition_training)は `#[cfg(feature = "wgpu")]` で限定し、CPU 推論経路(recognition ほか)は常時公開。bin には `required-features = ["wgpu"]` を付与し、wgpu 無効時はビルド対象外にする。
  - pose_extractor は `transformer_burn = { path = "../transformer_burn", default-features = false }` で取り込み、wgpu バックエンド一式のコンパイルを回避。
  - 検証: 軽量 lib のみ `cargo check --no-default-features` 30秒(wgpu/naga/wgpu-hal 非コンパイル)/ pose_extractor `cargo check` 47秒(同上)/ transformer_burn `cargo test`(default=wgpu)全15件パス・回帰なし。学習・推論の機能と精度は不変(burn のバックエンド差し替えのみ)。
- **未見テイク評価ハーネス 完了(2026-06-15)**:
  - `transformer_burn --eval-holdout <dict.json> [--holdout-per-label N]`。pose dict を**テイク単位で train/held-out に分割**(各ラベルでテイク番号の後ろ N 件=既定1を未見に)→ train だけで学習 → **未見テイクで top-k を測る**。Phase 0a の合否判定を1コマンド化(従来は train/predict 用に dict を手で2つ作る必要があった)。
  - `pose_data.rs` に `take_from_stem` / `load_holdout_split`(+ユニットテスト)、`main.rs` に `--eval-holdout` 分岐を追加。学習・評価は既存 `train_recognition` / `evaluate_topk` を再利用。
  - ドライラン(既存6動画、各語 take3 を未見): train=4 / 未見=2 に正しく分割・学習・評価。top-1 1/2・top-5 2/2(2クラス極小なので配線確認。ただし take3 は学習未使用=**従来の in-sample と違い真の out-of-sample**)。
  - テイクが N 以下のラベルは held-out を作れないため train のみに入れ評価対象外(警告)。
- **オーバーレイ可視化ツール 完了(2026-06-21、PR #6)**:
  - pose-extract「動画から姿勢/手を抽出」のオーバーレイを強化。手ランドマーク21点に**骨格線(MediaPipe 21本のボーン)**を描き、指の曲がりを目視できるように(回転アラインメント PR #5 の効果確認用)。S6 の教訓「数値でなく目で確かめる」の本丸で、撮影開始後の手トラッキング品質チェックの主導線。
  - 出力形式に**オーバーレイ動画 mp4(全フレーム)**を追加(従来はサンプル PNG 数枚のみ)。全フレームを `frame_%05d.png` で吐き ffmpeg で `overlay.mp4` に束ねる。描画を抽出ループ内へ移し生フレームの全バッファ(`Vec<Array3<u8>>`)を廃止=メモリ削減。ゼロフレーム時は動画化をスキップし警告に降格。
  - 実装は `pose_extractor/src/main.rs`(`HAND_CONNECTIONS` / `draw_line` / `encode_overlay_video`、`RunConfig.overlay_video`)。ユニットテスト(骨格トポロジ・線描画)+ 実モデルでの E2E スモーク `overlay_video_smoke`(`#[ignore]`)を同梱。CLI.md 更新。サブエージェントレビュー対応済み(ゼロフレーム致命化の是正・骨格トポロジテスト追加ほか)。
- **Phase 0a 終了基準を先頭10語の実撮影データで判定(2026-07-02、実質クリア)**:
  - 撮影セッションツールで先頭10語×3テイク(「ok」判定分、計30本)を撮影 → 手動で `<romaji>-<rep>.mp4` のフラット命名に整理(build-dict は `<タグ名>.mp4` のフラット構成が前提で、`raw_jsl` の `<word_id>_<romaji>/<rep>.mp4` というネスト構成とは形式が違うため)→ `build-dict` でポーズ辞書化(`transformer_burn/data/pose_dict_stage1.json`、30タグ×10フレーム×126次元)→ `--eval-holdout`(各語 train=2/held-out=1)で評価
  - **結果: top-1 3/10(30%)、top-5 8/10(80%)**。学習lossは0.000178まで低下(20サンプルをほぼ暗記した状態での汎化を見ている)。外れた2語は `anata`・`watashi`
  - **外れた2語には具体的な原因の見立てあり**: build-dict の左右手カバレッジを見ると、`anata` は学習2本とも右手優位(R41%/R35%)なのに held-out だけ左手優位(L36%/R0%)。126次元特徴が「左手枠/右手枠」で構成されているため、held-out テイクだけ学習時と違う枠にデータが来て別物に見えた可能性が高い(予測は `arigatou` 0.97、`onegaishimasu` 1.00 と、際どい誤りでなく高確信度の誤りである点も整合)。`konnichiwa` など両手を安定して使う語は正解しており、単一モデル能力の問題というより**撮影/検出時の左右手ゆれ**が主因と見ている
  - **判断(2026-07-02 ユーザー確認済み)**: 80%は実質クリア(2語の外れに具体的な原因があり、パイプライン自体は機能していると判断)として **Phase 0a は実質クリア**。ただし n=10 の極小サンプルなので数値の頑健性は低い。左右手ゆれの根本対応(特徴表現の見直し、または anata/watashi の再撮影)は**いったん保留**し、stage 2(11-20語目)の撮影を優先。同じ問題が再発するか様子を見てから対応を判断する
  - 学習済みモデルは `--save` 未指定のため未保存(評価専用の使い捨て実行)。実運用モデルが要る場合は保留分含む全データで `--train-pose` から再学習する必要がある
- **左右手ゆれの真因判明+診断ツール `--inspect-dict` 追加(2026-07-02)**:
  - transformer_burn に診断専用 CLI `--inspect-dict <dict.json>` を追加(学習なし・モデル不要)。pose dict をラベル別に集計し、テイクごとの左右手カバレッジと「使われた手」(L/R/LR/-、カバレッジ20%以上で使用扱い)を表示し、テイク間で使われた手が食い違うラベルを警告する。実装は `pose_data.rs`(`used_hands` / `inspect_dict`)+ `main.rs` の早期分岐。ユニットテスト2件追加
  - stage 1 の pose dict を点検した結果、**10語中7語で使用手がテイク間で食い違い**(anata, arigatou, ohayou, onegaishimasu, sayounara, sumimasen, watashi)。食い違いゼロの3語(konnichiwa・konbanwa・otsukaresama)はいずれも両手サイン。**top-1 30% はほぼ「使用手の train/holdout 不一致」で説明可能**で、上記「外れた2語」の見立てが実は7語に及んでいた
  - sayounara(テイク01=右手 / 03=左手)と watashi(01=左手 / 02=右手)の生動画フレームを目視検証し、**handedness 検出は正しく、撮影者がテイクごとに実際に手を持ち替えていた**ことが真因と確定。「検出時のゆれ」ではなく「撮影時のゆれ」であり、モデル・抽出パイプラインの品質問題ではない
  - **方針(2026-07-02 ユーザー決定)**: 実運用で利用者の使用手が揺れることも見据え、**使用手は固定せず、あえて一部左手データを含めて撮影を続ける**。stage 1 の混在7語は様子見(撮り直しなし)。撮影セッションへの使用手警告機能は追加しない。評価時は `--inspect-dict` で使用手分布を把握した上で数値を解釈する
  - あわせて words.tsv の採用語彙をユーザーが修正(手話で同一表現の語の統合=are-sore、別表現になる語の分割=kare/kanojo など。撮影済み 001〜010 は不変)。word_id 022 は一時 `???` プレースホルダだったが、**ユーザー決定で tatoeba(例えば)に確定**(2026-07-02。テンプレは maamaa だった)
- **build-dict 用エクスポート機能 完了(2026-07-02)**:
  - pose-extract メニュー「撮影テイクを build-dict 用にエクスポート」を追加。raw_jsl の `<word_id>_<romaji>/<rep>.mp4` から build-dict が読めるフラット構成 `<romaji>-<rep>.mp4` への並べ直しを自動化(stage 1 では手作業だった工程。stage 2 以降は毎回発生するため)
  - 規則: index.tsv 同期後、`quality_flag` が `ng` で始まるテイクを除外(進捗表示の有効テイクと同じ規則)。**テイク番号は raw_jsl の番号を保持**(欠番があっても付け替えない=遡及可能性優先。stage 1 の手動整理と同じ流儀)。ハードリンク(失敗時コピー)で raw_jsl 側には書き込まない。stage フィルタ可・そのまま build-dict へ連続実行可
  - 実装: `progress.rs`(`run_export` / `should_export` / `export_file_name` / `list_stages`)+ `main.rs`(`export_wizard`)。ユニットテスト3件+実データスモーク `export_stage1_smoke`(`#[ignore]`、stage 1 のエクスポート結果30本が既存 pose_dict_stage1.json の30タグ名と**完全一致**することを確認済み)
- **build-dict / 認識の前処理高速化(2026-07-15)**:
  - `extract_tag_features` が全フレームに Palm+Hand 推論をかけてから 10 フレームへ間引いていた無駄を解消。間引きインデックスは総フレーム数だけで決まる純関数なので、ffprobe の nb_frames から先にインデックスを決め、**選択フレームだけ推論**する(`extract_frames_filtered` で非選択フレームはバッファクローンごとスキップ)。nb_frames 不明時は従来の全フレーム経路にフォールバック、nb_frames 過大申告で実フレームが足りない場合は直前フレームの特徴で埋める(`assemble_selected`、ユニットテスト付き)
  - **sequence は従来と完全一致**(stage 1 全30テイクで一致を実測。等価性スモーク `selected_frames_equivalence_smoke`、stage 1 回帰スモーク `build_dict_stage1_sequence_regression` を `#[ignore]` で同梱)。処理時間は 1 本あたり約 3 倍高速(0205-01.mp4 実測 19.1秒 → 5.7秒。残りはデコード+パイプ転送)。この経路は build-dict と認識(動画→タグ)の両方が通るため、以後の全撮影ステージと認識レイテンシに効く
  - **coverage は「選択フレームのみ」基準に変更(2026-07-15 ユーザー決定)**: 学習特徴になるフレームそのものの手カバレッジになり診断として的確だが、粒度は 10% 刻みになり**旧 dict の全フレーム基準の数値とは直接比較不可**。dict metadata に `coverage_basis` フィールドを追加(旧 dict には無い)。`--inspect-dict` の使用手判定・認識時の警告はこの新基準の値を読む
  - stage 1 dict は新基準で再生成済み(診断結果は不変: 10語中7語で使用手食い違い、ラベル集合も同一)。旧 dict は `transformer_burn/data/pose_dict_stage1.fullframe-coverage.bak.json` に保管
  - 撮影セッションの手検出チェック(ng_hands 判定)は方式・閾値とも不変。非サンプルフレームのクローン削減のみ
- **stage 2 撮影・評価 完了(2026-08-01)**:
  - words.tsv をユーザーが編集: 語彙統合・分割(hai/iie→atteru/chigau、hanasu→iu など)+新規語追加で、stage 2 は 011〜051 の**41語**に拡張(当初想定の「11-20語目」より広い)。stage 1(001〜010)は不変
  - 41語×3テイク(計123テイク、一部NG撮り直し込み)を撮影 → エクスポート → build-dict で `transformer_burn/data/pose_dict_stage2.json` を作成(pose-extractの対話ウィザードはTTY必須のためユーザーが実行、以降の学習・評価コマンドは代行)
  - **stage 2 単独で `--train-pose --save models/rec_stage2`**: in-sample top-1/top-5 ともに 100%(123/123)。**暗記の確認であり汎化の証拠ではない**点は従来通り
  - **`--eval-holdout`(既定: テイク3を未見)は 41語中 39語で top-1 5%(2/41)・top-5 24%(10/41)** と stage 1(30%/80%)から大幅悪化。**原因を特定**: `--inspect-dict` で確認したところ、**stage 2 の41語中34語(83%)が「テイク1・2=右手、テイク3=左手」という同一パターン**(2026-08-22 追記: 全51語で測ると46語。数値が食い違って見えるのは測定範囲の違いで、どちらも正しい)(ユーザーへの聞き取りで撮影時の癖と確認)。`--eval-holdout` の既定分割(テイク番号の後ろ1件を毎回未見にする)と噛み合い、ほぼ全語で「学習=右手のみ、未見=左手のみ」という体系的なズレが発生し、この評価では正しく汎化性能を測れていない。**モデルの学習能力不足という証拠ではない**(左右手ゆれの根本原因は既知。[[stage1-hand-switching-finding]] 参照)
  - stage 1(30テイク)+ stage 2(123テイク)を単純結合した `data/pose_dict_full.json`(153テイク、metadata完全一致・キー重複なし)で **`--train-pose --save models/rec_full`** を実行。in-sample top-1/top-5 とも 153/153(100%)。**51語分の実運用候補モデルとして保存済み**(こちらも暗記の確認であり未見精度は未測定)
  - **未確定のまま残っている論点**(ユーザーが決めていない): `--eval-holdout` の分割方法の見直し(ランダム化 or 左右手が混ざるような分割)をやるか、ミラー拡張(L/R枠スワップ+x反転)などの特徴表現側対応をやるか、それとも今回の評価の限界として割り切るか
  - stage 3 という区分は無い: `docs/development-plan.md` の当初想定(stage 3〜5=21-50語目を分割撮影)は、実際には全部まとめて「stage 2」として撮り終えたため**発生しなかった**。追加で撮れる単語は現時点でなし
- **ミラー拡張(左右反転)を実装・効果を実測(2026-08-01)**:
  - stage 2 の `--eval-holdout` 急落([[stage2-eval-holdout-pitfall]])を受け、上記「未確定のまま残っている論点」のうち**ミラー拡張**を先行実装(評価分割のランダム化は未着手のまま)。`transformer_burn` に `--mirror-augment` フラグを追加(`pose_data.rs`: `mirror_features` / `with_mirror_augmentation`)。レイアウト(左手枠[0..63]+右手枠[63..126]、各21点×xyz、x はwidth正規化)に基づき、**枠を入れ替え+x座標を反転**(y,zはそのまま)。未検出(全ゼロ)の枠はゼロのまま反転しない。**学習側(train)のみに適用し、held-out(未見テイク)には適用しない**(適用すると「解けたことにする」測定になるため)
  - `data/pose_dict_full.json`(51語・153テイク)で `--eval-holdout` を拡張なし/ありで2回ずつ実行して比較: 拡張なし top-1 2%・12%(2回)/ top-5 22%・27%、**拡張あり top-1 18%・25% / top-5 47%・59%**。top-5 の範囲は拡張なし(22-27%)と拡張あり(47-59%)で重ならず、改善は一貫している。stage 1(10語)でも top-5 70%→100%を確認
  - **重要な副次発見**: 学習にシード固定が一切なく、**同じコマンドの2回実行だけで結果が大きく揺れる**(拡張なし top-1 が2%→12%)。過去に記録した単発の評価数値(stage1 30%/80%、stage2 5%/24% など)も同程度のブレを含んでいた可能性があり、**単発実行を確定値として扱うのは元々危うかった**。シード固定や複数回平均などの対応は未着手・未確定
  - 実装のみ完了。`models/rec_full` などの保存済みモデルはミラー拡張なしのまま(上書き未実施)。次にどう進めるか(シード固定、本番モデルの再学習、評価分割のランダム化との併用など)は別セッションで議論予定
- **学習のシード固定を実装、ただし完全な再現性は未達(2026-08-14)**:
  - `--seed <u64>`(既定42)を追加し、`run_recognition` の冒頭でモデル構築前に `TrainingBackend::seed()`(= Burn の `Backend::seed`)を呼ぶ。バッチのシャッフルは元々していないため、揺れの発生源は重み初期化のみのはずだった
  - **効果は部分的**: 同一seedで2回実行しても loss history・予測確信度は一致しない(スモークdictで確認: epoch1 loss 4.86 vs 5.49、最終 loss 0.0055 vs 0.0176。top-1の正誤自体は両方とも一致)。原因は **wgpu(GPU)バックエンドの浮動小数演算が並列実行順序に依存し非決定的**なため(加算の非結合性)。`Backend::seed()` は重み初期化の乱数列は固定するが、その後の行列積・reduction の実行順序までは固定しない
  - **CPUバックエンド(NdArray)への切替は現状不可**: 完全決定性の仮説検証のため `recognition_training.rs`(`train_recognition`/`compute_loss`/`evaluate_topk`)を `TrainingBackend` 決め打ちから `B: AutodiffBackend` ジェネリックに変更し(この変更自体は害がないため採用・コミット)、`Autodiff<NdArray>` で同じ学習ループを試したところ、**burn-ndarray 0.18.0 の `mask_where` 実装が現モデルの形状で `Option::unwrap()` パニックする**(Causal Self-Attention のマスク適用箇所)。ndarray バックエンド側のバグとみられ、本リポジトリ側の対応範囲外
  - **結論**: 重み初期化のseed固定は実施・コミット済み(再現性の改善であり無駄ではない)が、**wgpu を使う限り実行ごとの数値の揺れは残る**。単発実行の評価数値は今後も「参考値」として扱い、比較が必要な判断(pipe-v2基準測定など)は複数回実行して範囲で見る運用を継続する。ndarray バックエンドのバグを踏まえた対応(burn のバージョン更新待ち/Issue報告/回避策の実装)は未着手・未確定
- **pipe-v2 基準測定(2026-08-14、複数回実行)**:
  - `data/pose_dict_full.json`(51語153テイク)で `--eval-holdout` を実行(既定epochs=300、seed=1〜3)。
    **ミラー拡張あり**: top-1 27-29%(15/51, 14/51, 14/51)/ top-5 51-65%(29/51, 33/51, 26/51)。
    **ミラー拡張なし**(比較用、seed=1,2): top-1 8-10% / top-5 20-24%
  - **pipe-v2 基準(top-1 70% / top-5 90%)とのギャップは依然大きい**。ミラー拡張ありでも
    top-1 は目標の半分未満、top-5 も届いていない。seed固定後も実行間で top-1 27-29%・top-5
    51-65% とブレは残るが、ミラー拡張の効果自体は一貫している(なし: 8-24% → あり: 27-65%)
  - 外した語を見ると `sumimasen→onegaishimasu`、`taberu→konnichiwa/taberu自身が別語の予測に化ける`、
    `yon→ok/kanojo/are-sore` など、**高確信度(0.7〜1.0)の取り違えが目立つ**。原因の切り分け
    (左右手ゆれ以外の要因: 語彙51語での類似動作、テイク数3の少なさ、モデル容量不足など)は未着手
  - **現時点の判断材料**: pipe-v2 基準クリアには単純なミラー拡張だけでは足りない可能性が高い。
    テイク数増(3→5)・モデルスケールアップ・語彙の類似動作整理などのレバーの中でどれを引くかは
    ユーザー判断待ち(下記「未確定のまま残っている論点」参照)
- **次の撮影ラウンドを決定(2026-08-16)**: pipe-v2基準測定完了後の次レバーとして、**テイク数を3→10に引き上げ**(既存51語)、**語彙を51→70語に拡張**(新規19語: 疑問詞7=dare/nani/doko/itsu/naze/dou/ikutsu、時間表現5=kyou/ashita/kinou/ima/jikan、ニーズ・感情語5=itai/ureshii/kanashii/isogashii/nemui、人物関係2=kazoku/tomodachi。word_id 052〜070・stage 3)。**モデルスケールアップは今回は選択せず保留**(データ量がパラメータ数に対して極端に少ない状態でモデルを拡大しても改善しにくいため、データ主導で進めデータ増強後の頭打ちを見てから再検討する方針)。`transformer_burn/data/raw_jsl/words.tsv` を更新済み(全語 target_takes=10)
  - **左手比率は全体で約10%**(一般人口の左利き割合10.6%の目安に合わせた)。既存の非対称サインは元々3テイク中1本が左手だったため、追加7テイクは全て右手で統一し、合計10テイク中1本(10%)を維持する
  - **追加撮影目標**: 既存51語で+357テイク、新規19語で190テイク(1語10テイク)、合計547テイク。実績ペース(stage 2実績: 約4本/日)で試算すると約137日かかり、2026年内の残り日数とほぼ一致(バッファはほぼ無い)
  - **【2026-08-21 実測による訂正】上記の決定記録と `words.tsv` の実体がずれている**(決定そのものは変更していない。以下は実ファイルの実測値)。
    - 語彙は **74語**(stage1=10 / stage2=41 / **stage3=23**、word_id 052〜074)。決定記録の「70語・新規19語・052〜070」より4語多い
    - stage 3 の内訳の実体は 疑問詞8(dare/nani/doko/itsu-nanji/itsu-nangatunannnichi/naze/douyatte/ikura)+ 時間表現5(kyou/ashita/kinou/raishuu/sennshuu)+ 人物関係10(kazoku/tomodachi/chichi/haha/ani/otouto/ane/imouto/sohu/sobo)。**決定記録にある「ニーズ・感情語5(itai/ureshii/kanashii/isogashii/nemui)」と ima/jikan は words.tsv に存在しない**。2026-08-16 の記録後に words.tsv がさらに編集されたとみられる
    - 撮影目標は 74語×10 = **740テイク**。`index.tsv` の撮影済み(quality_flag=ok)は153テイクなので、追加は **約587テイク**(記録の547テイクは70語前提の数字)
    - **実績ペース「約4本/日」は `index.tsv` の実態と異なる**。153テイクはすべて2日間に集中しており、2026-07-02 に37本、2026-08-01 に131本。経過日数で割った平均であって、1日あたりの撮影能力ではない。所要日数の試算は前提から見直しが必要
  - **未確定のまま残る項目**: `--eval-holdout` のsplitランダム化(撮影癖と評価分割の噛み合わせで精度を誤測定するリスクは既知、[[stage2-eval-holdout-pitfall]])、ミラー拡張を本番モデル(`models/rec_full`)へ反映するか、モデルスケールアップの要否(データ増強後に判断)
  - タグ → 自然な日本語(部品C)は LLM 整形を後段で(①プロンプト→②LoRA→③自作の順で安く試す)。**①の試作+実データでの独立再検証まで完了**(`llm_shaping/` 参照)。②は文(複数サイン)認識が動いてから
  - 前提: S6 のランドマーク品質問題への取り組み継続。ただし認識は手ポーズ(C 系)中心で進められる

#### 2026-08-22: 撮影ゼロで top-1 27-29% → 68.8%(実験1〜6)

**1日で実施した6本の実験の結果。追加撮影は1本もしていない。**
各実験の詳細は `docs/experiments/` 配下の個別ドキュメントを参照。

| # | 実験 | 結果 | 記録 |
|---|---|---|---|
| 1 | 認識モデルの縮小 | **効果なし**。base が最良で、縮小は改善しない。容量は律速ではない | `model-size-eval.md` |
| 2 | 手形記述子を入力特徴量にする | **当たり**。生の座標126次元 → 関節角ベースの66次元で top-1 27-29% → 47-55% | `handshape-features-eval.md` |
| 3 | z-score標準化・学習設定・成分アブレーション | **標準化は当たり**。top-1 57-67% / top-5 78-90%。**手首の画像内座標 xy を落とすと明確に悪化 = 位置情報が効いている** | `handshape-tuning.md` |
| 4 | Palm検出のROIクロップ(タイル分割) | **診断は外れ、施策は当たり**。下記参照 | `palm-detection-roi.md` |
| 5 | 人物検出を前段に追加 | **外れ**。ただし副産物で CenterSquare が最良と判明。下記参照 | `person-detection-roi.md` |
| 6 | サイン区間からのフレーム選択 | **外れ**。実効フレームは2.35倍になったが精度は不変 | `sign-span-frame-selection.md` |

**現在の最良構成と数値**(handshape記述子 + z-score標準化 + CenterSquare、`--eval-holdout`、20 runs):

```
top-1 35.1/51 = 68.8%(レンジ 61-76%、sd 2.73件)
top-5 45.1/51 = 88.4%
pipe-v2 基準(top-1 70% / top-5 90%)には未達だが、朝の 27-29% / 51-65% からは大幅に前進
```

**手検出率の低さの主因が判明(実験4)**: 従来「10フレーム中3.57しか手が取れない」を検出失敗と解釈していたが誤り。
動画を時間方向に10等分した検出率は**きれいな∩字**(位置0=1.3% / 位置3=96.7% / 位置8-9=0.0%)で、
**手が写っている区間に限れば検出率は95.4%、取りこぼしは0.9%**しかない。
未検出フレームの大半は、一人撮影で録画開始/停止のラグがあるため**単に手が写っていない**フレームだった
(この撮影ラグ自体はどうしようもない制約として受け入れる、とユーザーが明示)。
**検出率という中間指標で判断していたために、実験4は有効な施策(CenterSquare)を一度捨てていた。**

**ROI モードの比較(実験5、独立2標本)**:

| ROIモード | top-1 平均 | top-5 平均 | Palm推論 | 幅のカバー |
|---|---|---|---|---|
| full-frame(**現在の既定**) | 58.6% | 82.5% | 1回 | 全幅 |
| **CenterSquare** | **68.8%** | **88.4%** | 1回 | 66.7%(手の3.7%が切れる) |
| person(人物追従) | 62.7% | 87.5% | 1回+人物検出 | — |
| tiles3 + 重複統合 | 66.1% | 88.6% | 3回 | 全幅 |

- CenterSquare − full-frame: top-1 **+10.2pt**(p<0.001)
- tiles3 − CenterSquare: **差の証拠なし**(top-5 p=0.86)= 3倍のPalm推論が何も買っていない
- 人物追従は固定正方より**有意に悪い**(top-1 −3.1件、p≈0.001)

**CenterSquare のクロップ範囲外に出る手の分析**(判断材料、独立に2回再現):

- 手の21点のいずれかがクロップ外に出る割合 **3.7%**(25/682件)。影響を受けるのは **10/51語**
- 余裕の分位点: 90%点で許容幅の **88.4%**、95%点で **96.4%** を使用 = **ほぼ余裕なし**
- 危険群は **方向・移動・指示を表す語**(iku 60% / are-sore 36% / ni 30% / kuru / sayounara)。
  「両手を左右に開く語」ではなかった
- 原因は立ち位置ではなく**利き手の外側への伸展**(右手は画像の左側に写る)。
  最適な固定オフセットは -16px しかなく効果もほぼゼロ、撮影日をまたぐと符号すら反転する(stage1: +7px / stage2: -16px)
- **使用手を固定せず左手を約10%含める方針([[stage1-hand-switching-finding]])のため、両側に余裕が必要**。片側に寄せる対策は原理的に筋が悪い

**S6(身体ランドマーク破綻)は直る見込み(実験5、目視のみ)**:
人物検出で得た ROI を**フレーム外にはみ出したままクランプせずに** BlazePose へ渡すと、
**顔のランドマークは顔に、肩は両肩に正しく乗る**。現行の中央正方クロップでは39点すべてが顔と襟元に密集する
= S6 の破綻そのもの。confidence は 0.731 vs 0.730 で**数値では区別できない**(S6 の教訓「数値でなく目で確かめる」の再現)。
身体が取れれば「顔・肩を基準にした手の相対位置」が使えるようになり、実験3で効くと分かっている位置情報を強化できる。
**実装・評価は未着手。**

**方法論の訂正**: `--seed` は学習結果を固定しない。同一コマンド・同一シードの反復で top-1 が 28〜34件(sd 約1.5件)まで振れる。
`handshape-tuning.md` にあった「同一 seed = 同一の重み初期化なので対比較の方が強い」という記述は削除・訂正済み。
**判断は変わらない**(符号検定は対であることを要求しないため妥当性は維持。失われたのは検出力)。
以後の比較は**独立2標本検定 + 10 runs 以上 + レンジ併記**で行う。

**未確定(すべてユーザー判断待ち。この節は事実の記録であって決定ではない)**:

- **ROI の既定を変えるか**(現在は full-frame のまま。CenterSquare は +10.2pt だが将来の方向・指示語で切れるリスク、
  tiles3 は切れないが3倍コスト、適応的切り替えという第3案もある)
- サイン区間フレーム選択(実験6)を残すか消すか(既定オフで実装済み。コスト2.33倍で効果なし)
- 人物検出モデルを S6 対応のために残すか
- 本番モデル `models/rec_full` の再学習(ユーザーが「待機」と明示)
- **2026-08-16 の「テイク数 3→10・追加587テイク」決定の見直し**。
  今日1日の撮影ゼロの変更(+40pt)が、587テイク(実績ペースで4〜5日の撮影)より確実に大きい効果を出したため、
  撮影より前処理・特徴表現に投資する方が効率が良い可能性がある。**まだ決めていない**

---

## 技術スタック

- **言語**: Rust
- **フレームワーク**: Burn 0.18.0
- **バックエンド**: Wgpu(GPU)、Autodiff(自動微分)、NdArray(CPU)
- **アーキテクチャ**: Seq2Seq Transformer(Encoder-Decoder、Pre-LN方式)
- **最適化**: Adam

### 現在のモデル設定
- **モデル次元**: d_model=64、2ヘッド(d_head=32)、d_ff=256(`transformer_burn/src/config.rs` の const 実測値。2026-08-21 訂正: 従来ここに書かれていた `d_model=16 / d_head=8 / d_ff=64` は Phase 15b 当時の値のまま更新されていなかった)
- **層数**: Encoder 4層、Decoder 4層
- **シーケンス長**: 10トークン(初期)
- **訓練設定**: バッチサイズ128。学習率は足場 Seq2Seq が 0.00005(`LEARNING_RATE`)、認識モデルが 0.0005(`POSE_LEARNING_RATE`)と別々
- **認識モデルのパラメータ数**: 約482K(上記寸法+51語語彙での概算)。学習サンプルは153件なので、パラメータ数はサンプル数の約3000倍

---

## コミュニケーション指針(重要)

### 言語とスタイル
- 必ず日本語で回答
- 論理的で簡潔、実践的で実行可能
- 絵文字を使わない

### コード指示
- **行数を明示**: 「XXX行目に追加」「YYY行目を修正」と具体的に指定
- **Rust初心者を考慮**: 所有権、トレイト、ライフタイムなど分かりにくい部分は丁寧に説明
- **修正前後を明示**: 変更箇所を明確に提示
- **複数ファイル**: ファイル名と行数を両方明記

### ユーザー背景
- Next.js、TypeScript、Supabaseでの開発経験あり
- **Rust初心者**: 基本文法は理解、細かい仕様は学習中
- UI/UX設計、社会的価値を重視

---

## Phase 15b: Seq2Seq翻訳モデル(完了)

### 実装内容
- ✓ Encoder-Decoder統合(各4層)
- ✓ Self-Attention、Cross-Attention
- ✓ 可変長出力(SOS/EOSトークン、自己回帰生成)
- ✓ Teacher Forcing訓練

### 実装結果
- 訓練: 100エポック、Loss 15.3 → 1.3
- 推論例: 「ありがとう」→ `<ありがとう>`(正解)
- 制約: 小規模モデル(d_model=16)、少量データ(47サンプル)

---

## Phase 16: モデル保存・推論基盤(90%完了)

### 実装済み機能
- ✅ モデル保存/読み込み(BinFileRecorder、バイナリ形式)
- ✅ クロスプラットフォーム推論(WGPU/NdArray切り替え、autoモード)
- ✅ CLIフラグ(--train, --save, --load, --predict, --backend, --export-attn)
- ✅ メタデータ管理(config.json, metrics.json, README.md自動生成)
- ✅ CSVエクスポート機能(Attention行列、テンソル出力)
- ✅ テストコード(往復一致性、クロスプラットフォーム)

### 出力ディレクトリ構成
```
models/<timestamp>/
├── model.bin       # モデル本体(Burnバイナリ)
├── config.json     # ハイパーパラメータ、語彙情報
├── metrics.json    # 訓練統計、損失履歴
├── README.md       # 訓練メモ(自動生成)
└── exports/        # オプション:分析用CSV
```

### 未実装
- Attention行列の捕捉(モデル変更が必要)
- オプティマイザ状態の保存(継続訓練用)

---

## CLI使用例

### 基本コマンド
```bash
# 訓練
cargo run --release -- --train --save models/run001

# 推論
cargo run --release -- --load models/run001 --predict "こんにちは"

# バックエンド選択(auto: WGPU→NdArrayフォールバック)
cargo run --release -- --load models/run001 --backend auto --predict "ありがとう"

# Attention行列CSV出力
cargo run --release -- --load models/run001 --predict "おはよう" --export-attn
```

### elan_parser
```bash
cd elan_parser
cargo build --release
./target/release/elan-eaf-parse tests/fixtures/sample.eaf
./target/release/elan-eaf-parse <input.eaf> --tier <tier_id> --format json
```

---

## transformer_burn - 次のステップ

- [ ] テストコード実行確認
- [ ] 実際に訓練を実行してモデル保存を検証
- [ ] Phase 16a(長いシーケンス対応)またはPhase 16b(モデルスケールアップ)へ進む

---

## translator_ja_en - 次のステップ

### Phase 1〜3(完了)
- ✓ 日英対訳データセット準備(2879サンプル、`data/` 配下に複数ファイルへ分割: 基本文・挨拶拡張・文章拡張・英国英語表現など)
- ✓ 語彙モジュール実装(translation_vocabulary.rs、日本語168語 + 英語798語、分離語彙空間)
- ✓ データローダー実装(translation_data.rs)
- ✓ シーケンス長拡張(seq_len=30)
- ✓ モデルスケールアップ(d_model=128、4ヘッド、Encoder/Decoder各4層、d_ff=512)
- ✓ ビームサーチ推論実装(translation_model.rs::generate_with_beam_search、CLIの--beam-widthで貪欲探索/ビーム探索を切替)

### データセット例(data/translation_data_ja_en.txt)
```
こんにちは	Hello
ありがとう	Thank you
おはようございます	Good morning
私は学生です	I am a student
```

### Phase 4(実用化、残タスク)
- [ ] BLEU評価の実装
- [ ] 訓練済みモデル(`models/test/`、150エポック、final loss 0.162、2025-11-01訓練)の翻訳品質を定性チェック

---

## elan_parser - 次のステップ

- [ ] `REF_ANNOTATION`(階層 tier)対応
- [ ] Stereotype 処理
- [ ] 実データ(NHK STRL/NINJAL コーパス or 自前アノテーション)での検証
- [ ] `transformer_burn` 前処理パイプラインへの組み込み

---

## インフラ / 計算環境 移行メモ

- **【2026-08-30 更新・従来の「移行方針」は保留に変更】** 従来ここに書かれていた「手話動画の撮影完了後、開発機を MacBook → デスクトップ Windows PC(RTX 5070 Ti新規購入)に移行予定」は確定した移行計画ではなく、**現時点ではPC購入(Windows機・MacBook Pro買い替えとも)を保留**している。理由: 現状の実験負荷(51語153テイクの`--eval-holdout`が100+ runsでも約1時間)は所有機材の損益分岐点(概算約15,400時間)に遠く及ばず、経済合理性が無いため。加えて、当初移行予定だったWindows機自体が2026-08-15時点で「調子が良くない(使えないわけではない)」という状態(詳細な症状の記録なし、その後の経過も未確認)
- **当面の方針**: MacBook(Air M2)継続 + 必要な時だけクラウドGPU(RunPod等)を都度レンタル。クラウドへの切り替え基準は (1) 1回の学習/実験が24時間を超える (2) 発熱・性能劣化が目立つ (3) Airの専有が本業以外の個人利用を妨げる、のいずれか(2026-08-30時点、いずれも未該当)
- **PC購入(Windows機 or MacBook Pro)の再検討条件**: 上記クラウド切り替え基準に達し、かつ開発ロードマップで必要な計算規模(モデルサイズ・語数)が固まってから。それまでは月1万円を予備資金として積み立て中(Windows機相当40万円で約3.3年、MacBook Pro相当37〜60万円で約3.1〜5年の目安)
- **詳細・価格調査・損益分岐点の計算根拠は `docs/cloud-migration-scenario.md` を参照**(2026-08-16作成、2026-08-25・08-30に追記。git未追跡のため注意)
- **クラウド利用時の技術メモ**: Burn の wgpu バックエンドは Vulkan 経由で Linux+NVIDIA GPU を CUDA 未インストールでも動かせる見込みが高い(実機未検証)。CUDAバックエンドを使う場合は Blackwell(sm_120系)への CUDA Toolkit / ドライバ / Burn のバージョン対応を要確認

---

**最終更新**: 2026年8月22日(**撮影ゼロで top-1 27-29% → 68.8% / top-5 51-65% → 88.4%**: 実験1〜6を実施し、追加撮影を1本もせずに精度を大幅に改善した。効いたのは (1) 入力を生の座標126次元から手形記述子66次元へ、(2) z-score標準化、(3) Palm検出のROIを中央正方クロップに、の3つ。pipe-v2基準(top-1 70%/top-5 90%)には未達だが接近した。**手検出率の低さの主因は解像度ではなく撮影ラグ**と判明(時間方向の検出率が∩字。手が写っている区間に限れば検出率95.4%)。検出率という中間指標で判断していたため、有効な施策を一度捨てていた。**S6(身体ランドマーク破綻)は人物ROIをクランプせずBlazePoseへ渡せば直る見込み**(目視のみ、未実装)。**`--seed` は学習結果を固定しない**ことが判明し、比較は独立2標本検定+10runs以上へ移行。あわせてドキュメントと実体のずれを訂正(S6の解像度は当時1920×1080だが現データは1620×1080 / pose_extractorの依存にclapは無い / 語彙内訳のタグ数79 / translator_ja_enは1ファイル998行しか読んでいない / 使用手食い違いの測定範囲)。**ROIの既定は変更していない(full-frameのまま)。テイク数3→10の決定の見直しを含め、判断はすべてユーザー待ち**。詳細は pose_extractor 節の「2026-08-22」小節と `docs/experiments/` を参照。以前の更新(2026-08-21)=**ドキュメントと実体のずれを訂正**(方針・決定は変更なし): `explore/subword-decomposition` ブランチでのサブワード分解の実現可能性調査(`docs/subword-decomposition-proposal.md`)の副産物として、実ファイルで検証した事実の食い違いを修正した。(1) 「現在のモデル設定」の d_model は 16 ではなく **64**(d_head=32、d_ff=256。認識モデルは約482K params で学習サンプル153件)。(2) `words.tsv` の実体は70語ではなく **74語**(stage3=23語、word_id 052〜074)で、記録にある感情・ニーズ語5語は存在しない。(3) 追加撮影目標は547テイクではなく **約587テイク**(740−153)。(4) 撮影ペース「約4本/日」は実態と異なり、153テイクは2日間(37本/日・131本/日)に集中。なおサブワード分解自体は**探索段階で未確定**、Phase 2 は未着手。以前の更新(2026-08-16)=**次の撮影ラウンドを決定**: pipe-v2基準測定の結果を受け、テイク数を3→10に引き上げ(既存51語)、語彙を51→70語に拡張(疑問詞7語・時間表現5語・ニーズ/感情語5語・人物関係2語の計19語、word_id 052〜070・stage 3)。モデルスケールアップは保留しデータ主導で進める方針(データ量がパラメータ数に対し極端に少ない状態でのモデル拡大は避け、データ増強後の頭打ちを見てから再検討)。左手比率は一般人口の左利き割合(メタ分析ベース約10.6%)を目安に全体で約10%に設定(既存の非対称サインは元々3テイク中1本が左手だったため、追加7テイクは全て右手で統一し10テイク中1本を維持)。`transformer_burn/data/raw_jsl/words.tsv` を更新済み(全70語、target_takes=10)。追加撮影目標は既存51語+357テイク・新規19語+190テイクの計547テイクで、実績ペース(約4本/日)なら約137日=2026年内の残り日数とほぼ一致(バッファはほぼ無い)。`--eval-holdout`のsplitランダム化とミラー拡張の本番モデル(`models/rec_full`)への反映は引き続き未確定。撮影自体は未実施(次の実撮影セッションから)。以前の更新(2026-08-14)=**部品Cの実データ独立再検証を完了**: `llm_shaping/prompt_v1.md`の語彙表がstage 2の語彙整理に未追随だった同期漏れを修正(51語)。pipe-v2測定で得た実際のtop-k出力6件を、会話履歴を共有しないフレッシュな独立エージェントに1件ずつ実行させて検証。2026-07-02のTC1〜8と同じ結論(高確信度の単発誤りは部品Cでは救えない、拮抗?判定の閾値が不明確)が実データでも再現。`eval_v1.md`に追記。同日それ以前=**pipe-v2基準測定を複数回実行、目標とのギャップ大きいまま**: `pose_dict_full.json`(51語)で`--eval-holdout`をミラー拡張あり/なし・複数seedで実行。ミラー拡張あり top-1 27-29%/top-5 51-65%、なし top-1 8-10%/top-5 20-24%。pipe-v2基準(top-1 70%/top-5 90%)には遠く、単純なミラー拡張だけでは足りない可能性。テイク数増・モデルスケールアップ・語彙整理などレバーの選択はユーザー判断待ち。同日それ以前=**学習のシード固定を実装、ただし完全な再現性は未達**: `--seed`フラグを追加し`Backend::seed()`で重み初期化を固定したが、同一seedでも実行ごとにloss/確信度が異なることを確認。原因はwgpu(GPU)バックエンドの浮動小数演算の非決定性(重み初期化以外は固定できない)。`Autodiff<NdArray>`(CPU)への切替を試したが、burn-ndarray 0.18.0の`mask_where`が現モデル形状でパニックするバグを発見し断念。`recognition_training.rs`はバックエンドジェネリックに変更(この副産物はコミット)。単発の評価数値は今後も参考値として扱い、比較が必要な判断は複数回実行で行う運用を継続。以前の更新(2026-08-01)=**ミラー拡張(左右反転)を実装・効果を実測**: stage2の`--eval-holdout`急落を受け、`transformer_burn`に`--mirror-augment`フラグを追加(学習データの左右反転コピーを追加、held-out側には適用しない)。`pose_dict_full.json`(51語)で拡張なし/ありを2回ずつ比較し、top-5が22-27%→47-59%へ一貫して改善(重複しない範囲)。**重要な副次発見**: 学習にシード固定がなく同一コマンドの再実行でも結果が大きく揺れることが判明(拡張なしでtop-1が2%→12%)。過去の単発評価数値も同程度のブレを含んでいた可能性があり、次のシード固定対応や本番モデル(`models/rec_full`)への反映は別セッションで判断予定。以前の更新(2026-08-01)=**stage 2 撮影・評価 完了**: words.tsv をユーザーが編集し stage 2 は011〜051の41語に拡張。41語×3テイク撮影→build-dictで`pose_dict_stage2.json`作成。stage 2単独学習はin-sample 100%(暗記の確認)。`--eval-holdout`はtop-1 5%/top-5 24%とstage 1から大幅悪化したが、原因は「41語中34語でテイク1・2=右手、テイク3=左手」という撮影パターンとholdoutの既定分割(テイク3を毎回未見化)が噛み合った体系的なズレと特定(モデル能力不足ではない)。stage 1+2を結合した`pose_dict_full.json`(153テイク・51語)で`--train-pose --save models/rec_full`を実行しin-sample 100%、51語分の実運用候補モデルとして保存。holdout評価方法の見直し(分割ランダム化 or ミラー拡張などの特徴表現対応)は未確定でユーザー判断待ち。stage 3という区分は発生せず(当初計画のstage 3〜5相当をまとめてstage 2として撮り終えたため)、200語拡張(v0.3相当)は次回以降に先送り。以前の更新(2026-07-15)=**build-dict / 認識の前処理を高速化**: 全フレーム推論→間引きだった `extract_tag_features` を「選択フレームのみ推論」に変更し1本あたり約3倍高速化。sequence は完全一致(stage 1 全30テイクで実測)、coverage は「選択フレームのみ」基準へ変更(ユーザー決定)し stage 1 dict を再生成(診断結果は不変)。詳細は pose_extractor 節参照。以前の更新(2026-07-02)=**部品C(タグ→日本語)のプロンプト①を試作**: `llm_shaping/` を新設し、prompt_v1 + 検証記録8ケースを作成。top-k 挽回は top-1 の組が破綻する場合のみ発動するため②LoRA への投資は文認識の実装後で十分、という所見。同日: **build-dict 用エクスポート機能を追加**: raw_jsl → フラット構成の並べ直しを pose-extract のメニューに自動化。ng 除外+元テイク番号保持で、stage 1 の手動整理を完全再現できることを実データスモークで確認。words.tsv の word_id 022 はユーザーが「例えば」(tatoeba)に確定。同日それ以前: **左右手ゆれの真因判明**: 診断ツール `--inspect-dict` を transformer_burn に追加して stage 1 を点検した結果、10語中7語で使用手がテイク間で食い違い、生動画の目視で「検出誤りではなく撮影者が実際に手を持ち替えていた」ことを確定。方針は「使用手は固定せず、あえて一部左手データを含める」(ユーザー決定)。words.tsv の採用語彙も修正(are-sore 統合・kare/kanojo 分割など)。同日それ以前: **Phase 0a 終了基準を先頭10語の実撮影データで判定**: top-1 30%・top-5 80%で実質クリアと判断、次は stage 2(11-20語目)の撮影へ。あわせて撮影セッションの手検出チェックを「動画全体平均」から「連続して手が検出できた最長区間」ベースに変更(一人撮影だと前後の手ぶら時間で平均が構造的に閾値を割るため)。同日中の一連の変更: `translator_ja_en` 節のドキュメント訂正(実態より約8ヶ月古い記載だった`初期セットアップ完了`を、実際は2025-11-01時点でPhase 1〜3完了済みだった内容に修正)。直前の更新(2026-06-21)=**オーバーレイ可視化ツール**を pose-extract に追加(PR #6)。手ランドマーク21点に骨格線を描きオーバーレイを全フレームmp4で出力可能に。さらに前=Hand Landmark の回転アラインメント(PR #5)、ビルド軽量化(PR #4)、未見テイク評価ハーネス `--eval-holdout`(PR #3)、認識方向パイプライン S1〜S8 + 動画→タグ直結パス(PR #1)を main にマージ済み)
